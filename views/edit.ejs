<!-- Container to hold the form for editing an assignment -->
<div class="container">
    <!-- Page Title -->
    <h1>Edit Assignment</h1>

    <!-- Form to update the assignment, method is PUT -->
    <form action="/assignments/<%= assignment._id %>?_method=PUT" method="POST">

        <!-- Input field for the assignment title -->
        <div class="form-group">
            <label for="title">Title</label>
            <input type="text" id="title" name="title" class="form-control" value="<%= assignment.title %>" required>
        </div>

        <!-- Input field for the subject of the assignment -->
        <div class="form-group">
            <label for="subject">Subject</label>
            <input type="text" id="subject" name="subject" class="form-control" value="<%= assignment.subject %>" required>
        </div>

        <!-- Input field for the due date, using type="date" -->
        <div class="form-group">
            <label for="dueDate">Due Date</label>
            <!-- The value is formatted to match the date input's expected format (YYYY-MM-DD) -->
            <input type="date" id="dueDate" name="dueDate" class="form-control" value="<%= assignment.dueDate.toISOString().substring(0, 10) %>" required>
        </div>

        <!-- Textarea for the assignment description -->
        <div class="form-group">
            <label for="description">Description</label>
            <textarea id="description" name="description" class="form-control" required><%= assignment.description %></textarea>
        </div>

        <!-- Dropdown for the status of the assignment, with options "Pending" and "Completed" -->
        <div class="form-group">
            <label for="status">Status</label>
            <select id="status" name="status" class="form-control">
                <!-- Conditionally mark "Pending" or "Completed" as selected based on the current status -->
                <option value="Pending" <%= assignment.status === 'Pending' ? 'selected' : '' %>>Pending</option>
                <option value="Completed" <%= assignment.status === 'Completed' ? 'selected' : '' %>>Completed</option>
            </select>
        </div>

        <!-- Submit button to update the assignment -->
        <button type="submit" class="btn btn-primary">Update Assignment</button>
    </form>
</div>